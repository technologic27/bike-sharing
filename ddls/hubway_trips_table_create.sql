CREATE TABLE hubway_trips (
    seq_id text,
    hubway_id text,
    status text,
    duration text,
    start_date text,
    strt_statn text,
    end_date text,
    end_statn text,
    bike_nr text,
    subsc_type text,
    zip_code text,
    birth_date text,
    gender text,
    PRIMARY KEY (seq_id)
);